'use strict';

describe('Service: GrowModule', function () {

  // load the service's module
  beforeEach(module('virusOneApp'));

  // instantiate service
  var GrowModule;
  beforeEach(inject(function (_GrowModule_) {
    GrowModule = _GrowModule_;
  }));

  // it('should do something', function () {
  //   expect(!!growModule).toBe(true);
  // });

});
