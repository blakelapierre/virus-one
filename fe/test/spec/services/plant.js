'use strict';

describe('Service: Plant', function () {

  // load the service's module
  beforeEach(module('virusOneApp'));

  // instantiate service
  var Plant;
  beforeEach(inject(function (_Plant_) {
    Plant = _Plant_;
  }));

  // it('should do something', function () {
  //   expect(!!plant.js).toBe(true);
  // });
});
